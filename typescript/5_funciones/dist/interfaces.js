"use strict";
//Interfaces
var point = {
    x: 5,
    y: 4
};
function printPoint(_point) {
    console.log("el punto en el plano es (" + _point.x + "," + _point.y + ")");
}
printPoint(point);
